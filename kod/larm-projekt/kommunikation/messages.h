#ifndef MESSAGES_H
#define MESSAGES_H

enum MSG_ID {
	ALARM,
	ALARM_OFF,
	POLL_REQUEST,
	POLL_RESPONSE,
	DICP_REQUEST, // Dynamic ID Configuration Protocol
	DICP_RESPONSE,
	TOL_SET,
	ACTIVE_ON,
	ACTIVE_OFF,
}

#endif